import React from 'react';

interface IProps {
    label?: string
    page?: string
    child?: string
}

export function CalendarPage({label, page, child}: IProps) {
    return (
        <div className={"page"}>
            Calendar 11111
        </div>
    )
}