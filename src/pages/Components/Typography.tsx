import React from 'react';
import {IPageProps} from "../../utils/interfaces/pages";


export function Typography({label, url, isRaised}: IPageProps) {
    return (
        <div className={"page"}>
            Typography
        </div>
    )
}