import React from 'react';
import {IPageProps} from "../../utils/interfaces/pages";


export function Icons({label, url, isRaised}: IPageProps) {
    return (
        <div className={"page"}>
            Icons
        </div>
    )
}