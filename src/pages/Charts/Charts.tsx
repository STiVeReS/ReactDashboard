import React from 'react';
import {IPageProps} from "../../utils/interfaces/pages";


export function Charts({label, url, isRaised}: IPageProps) {
    return (
        <div className={"page"}>
            Charts
        </div>
    )
}