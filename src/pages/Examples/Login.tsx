import React from 'react';
import {IPageProps} from "../../utils/interfaces/pages";


export function Login({label, url, isRaised}: IPageProps) {
    return (
        <div className={"page"}>
            Login
        </div>
    )
}