import React from 'react';
import {IPageProps} from "../../utils/interfaces/pages";



export function Register({label, url, isRaised}: IPageProps) {
    return (
        <div className={"page"}>
            Register
        </div>
    )
}