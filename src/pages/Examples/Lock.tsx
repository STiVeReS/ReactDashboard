import React from 'react';
import {IPageProps} from "../../utils/interfaces/pages";


export function Lock({label, url, isRaised}: IPageProps) {
    return (
        <div className={"page"}>
            Lock
        </div>
    )
}