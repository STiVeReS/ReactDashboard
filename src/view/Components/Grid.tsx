import React from 'react';
import {IPageProps} from "../../utils/interfaces/pages";


export function Grid({label, url, isRaised, nav}: IPageProps) {
    return (
        <div className={"page"}>
           Grid
        </div>
    )
}