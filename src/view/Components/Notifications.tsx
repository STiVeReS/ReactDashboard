import React from 'react';
import {IPageProps} from "../../utils/interfaces/pages";


export function Notifications({label, url, isRaised, nav}: IPageProps) {
    return (
        <div className={"page"}>
            Notifications
        </div>
    )
}