import React from 'react';
import {IPageProps} from "../../utils/interfaces/pages";

export function Cards({label, url, isRaised, nav}: IPageProps) {
    return (
        <div className={"page"}>
            Cards
        </div>
    )
}