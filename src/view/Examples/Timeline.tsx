import React from 'react';
import {IPageProps} from "../../utils/interfaces/pages";


export function Timeline({label, url, isRaised, nav}: IPageProps) {
    return (
        <div className={"page"}>
            Timeline
        </div>
    )
}