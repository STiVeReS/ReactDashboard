export interface ISubmitForm {
    email: string;
    password: string
}

export interface IFormTitle {
    title?: string;
    link?: string;
}

export interface IDefaultForm {
    email?: string;
    password?: string;
}